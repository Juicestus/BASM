( Code to print all the numbers up to 100 ) 

( set new variable counter to 0 )
	: set counter 0. ;			

( add 1 to counter, also a goto pointer )
pointer : add counter 1 -> counter ;		

( prints the counter, then a newline )
	: print counter '\n';			

( shouldJump is boolean for if counter is less than or equal to 100 )
	: lteq counter 100 -> shouldJump ;	
	
( if shouldJump evaluates to true, jumps to pointer defined earliers)	
	: jump shouldJump pointer ;		